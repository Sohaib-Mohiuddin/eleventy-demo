local_backend: true
  # url: http://cms-proxy:8081/   # enables local dev via cms-proxy container
backend:
  name: proxy     # local dev: git-gateway; for production switch to github/gitlab/bitbucket
  proxy_url: http://cms-proxy:8081/   # enables local dev via cms-proxy container
media_folder: "src/images"
public_folder: "/assets/images"

collections:
  - name: "posts"
    label: "Posts"
    folder: "src/posts"
    create: true
    slug: "{{slug}}"
    extension: "md"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "text"}
      - {label: "Body", name: "body", widget: "markdown"}
